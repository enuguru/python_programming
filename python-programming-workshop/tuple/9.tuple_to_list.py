

# Tuple containing unsorted odd numbers.
odds = (9, 5, 11)

# Convert to list and sort.
mylist = list(odds)
mylist.sort()
print(mylist)

# Convert back to tuple.
sorted_odds = tuple(mylist)
print(sorted_odds)
